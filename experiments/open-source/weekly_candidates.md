# Weekly OSS Candidate Issues

Generated by the `oss-candidates` skill (`.claude/skills/oss-candidates/`). Most recent run at
the top. Each run's 5 picks are drawn from the repo shortlist in `oss_contribution_targets.md`,
mechanically filtered (unassigned, ≤10 comments, not brand-new, not previously logged here) via
`scripts/fetch_candidates.py`, then judged for scope/claiming/linked-PR by hand.

---

## 2026-07-29

- **[ray-project/ray#63202] \[Core]\[Ray Client] specific_server intermittently dies during fork() — gRPC poller race in proxier](https://github.com/ray-project/ray/issues/63202)** — labels: `bug`, `P1`, `core`, `stability`, `community-backlog`, `contribution-welcome`. 0 comments, unassigned.
  Rationale: Detailed root-cause write-up of a real intermittent bug in Ray Client's proxier (gRPC poller race during fork). Scoped enough to attack (reproduction rate, mechanism) but needs real investigation — plausibly 1-2 weeks. Zero competition, nobody has touched it.

- **[pytorch/pytorch#85234] c10d all_gather aborts with Signal 8 (SIGFPE) when tensor.numel() == 0](https://github.com/pytorch/pytorch/issues/85234)** — labels: `oncall: distributed`, `good first issue`, `module: c10d`, `bot-triaged`, `oncall: distributed infra`, `ptd-bot-triaged`. 1 comment, unassigned.
  Rationale: Core distributed-training bug (gloo backend crashes on empty tensors in all_gather). The one comment is a maintainer noting NCCL doesn't hit it, not a claim. Squarely in training-systems internals, good resume fit.

- **[pytorch/pytorch#177371] LocalTensorMode: all_to_all_single crashes with uneven split sizes](https://github.com/pytorch/pytorch/issues/177371)** — labels: `oncall: distributed`, `good first issue`, `bot-triaged`, `oncall: distributed infra`, `ptd-bot-triaged`. 1 comment, unassigned.
  Rationale: Another unclaimed distributed-internals bug (LocalTensorMode reshape crash on uneven splits). The single comment is just a `cc` tag to reviewers, not a claim. Reasonable 1-2 week debugging+fix scope.

- **[ray-project/ray#59303] \[Type Enhance] The improvement of static check of `ray.method`](https://github.com/ray-project/ray/issues/59303)** — labels: `enhancement`, `P2`, `usability`, `core`, `community-backlog`, `contribution-welcome`. 1 comment, unassigned.
  Rationale: Maintainer (edoakes) explicitly confirmed "contributions in this area are definitely welcome" and gave a concrete constraint (Python ≥3.10 only). Typing/tooling work on `@ray.method`, no one has claimed it since the maintainer reply.

- **[sgl-project/sglang#1932] \[Feature] Save cache from requests and load](https://github.com/sgl-project/sglang/issues/1932)** — labels: `good first issue` (per repo list; not present in issue's own label set at fetch time — verify on GitHub), `inactive`. 2 comments, unassigned.
  Rationale: Maintainer (merrymercy) confirmed it's a wanted feature; the other comment is just a clarifying question, not a claim. Directly in SGLang's KV-cache path, matches the current `apps/triton-rt` work closely.

### Notes on this run
- Checked ~10 additional low-comment vLLM/SGLang issues (e.g. vllm#39030, #28407, #40544, #31414, #38246; sglang#8072, #6357, #12562, #19612, #8540, #5979, #9889) — **every one was already claimed in comments or had an open PR linked**, despite surviving the mechanical `assignees`-empty + low-comment-count filter. This is a real signal: popular vLLM/SGLang good-first-issues get claimed within hours, so the comment-based judgment pass in step 2 of the skill is doing real work, not a formality.
- No huggingface/peft, huggingface/accelerate, dmlc/xgboost, or ai-dynamo/dynamo issues survived this run's mechanical filter (their good-first-issue pipelines are thin right now per `oss_contribution_targets.md`) — accelerate#3078 did survive the mechanical filter but was excluded as an open-ended community thread, not a scoped task.
